/* eslint-disable import/no-anonymous-default-export */
const INITIAL_STATE = {

}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case 'value':
            
            return{
                
            };
    
        default:
            return state;
    }
}